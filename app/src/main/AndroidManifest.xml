<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.SchoolMobile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/SchoolMobile"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Disciplina_Aluno"></activity>
        <activity android:name=".DiaHorario" />
        <activity android:name=".EmentaPage" />
        <activity android:name=".Chat" />
        <activity android:name=".DisciplinasPage_Aluno" />
        <activity android:name=".HorarioPage" />
        <activity android:name=".HomePage"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>